<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/styles.css') }}" type="text/css"/>

    <title>{% block title %} {% endblock %}</title>
</head>
    <body>

        <header>
            <img src="\static/img/Logo.png" alt="Logo">
                <nav>
                    <a href="{{url_for('')}}">Iniciar Sesión</a>
                    <a href="{{url_for('')}}">Registrarse</a>
                    <a href="{{url_for('')}}">Salir</a>
                </nav>
        </header>
        
            
            <div class="banner">
                <img class="Banner_img" src="\static\img/Banner1.png" alt="Banner1"> 
            </div>
            

        <div class="content">
          {% block content %} {% endblock %}
        </div> 
    </body>
</html>